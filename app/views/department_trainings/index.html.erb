<h1>Listing department_trainings</h1>

<table>
  <tr>
    <th>Section</th>
    <th>Event</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @department_trainings.each do |department_training| %>
  <tr>
    <td><%= department_training.department_id %></td>
    <td><%= department_training.training_id %></td>
    <td><%= link_to 'Show', department_training %></td>
    <td><%= link_to 'Edit', edit_department_training_path(department_training) %></td>
    <td><%= link_to 'Destroy', department_training, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Section training', new_department_training_path %>
